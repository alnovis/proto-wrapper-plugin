syntax = "proto3";
package test;

// Scenario: No changes between versions
message StableMessage {
    int64 id = 1;
    string name = 2;
    repeated string tags = 3;
}

enum StableEnum {
    STABLE_ENUM_UNSPECIFIED = 0;
    STABLE_ENUM_VALUE1 = 1;
    STABLE_ENUM_VALUE2 = 2;
}
